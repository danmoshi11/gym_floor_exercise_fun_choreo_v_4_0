# 🎬 视频上传审核系统使用说明

## 🚀 快速开始

### 1. 安装依赖
```bash
pip install -r requirements.txt
```

### 2. 启动服务器
```bash
python server.py
```

### 3. 打开浏览器
- 主页: http://localhost:5000
- 管理后台: http://localhost:5000/admin

---

## 📋 功能介绍

### 用户上传视频
1. 点击任意动作卡片
2. 在弹窗右侧找到「上传视频」区域
3. 填写运动员姓名和国家（如 `Simone Biles`, `USA`）
4. 点击「选择视频文件」选择 mp4 视频
5. 点击「上传」按钮

### 管理员审核视频
1. 打开管理后台: http://localhost:5000/admin
2. 查看待审核视频列表
3. 可以预览视频、修改信息
4. 点击「✅ 批准」或「❌ 拒绝」
5. 批准后自动：
   - 重命名视频文件
   - 移动到正确目录
   - 更新 data.js

---

## 📁 目录结构
```
├── cards/
│   ├── cardimages/      # 动作封面图片
│   └── cardvideos/
│       ├── *.mp4        # 已审核通过的视频
│       └── pending/     # 待审核视频
├── app.js               # 前端逻辑
├── data.js              # 动作数据库
├── server.py            # 后端服务器
└── admin.html           # 管理后台
```

---

## 💡 注意事项
- 视频文件命名格式: `{动作ID}_{运动员}_{国家}.mp4`
- 国家代码建议使用 3 字母大写（如 CHN, USA, JPN）
- 批准视频后，刷新主页即可看到新视频
